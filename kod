#include <iostream>
#include <list>
#include <vector>
using namespace std;

int sum(vector<int> vect)
{
	int total = 0;
	for (int i = 0; i < vect.size(); i++)
	{
		total += vect[i];
	}
	return total;
}

int main()
{
	vector<int> my_list = {9, 2, 6, 1};
	int summa = sum(my_list);
	cout << summa;
}
